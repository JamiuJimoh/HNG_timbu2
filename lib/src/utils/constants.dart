import 'package:flutter/material.dart';

const commonWidth = 24.0;
const logoWidth = 99.0;
const commonPaddingH = EdgeInsets.symmetric(horizontal: commonWidth);
const commonPaddingHV =
    EdgeInsets.symmetric(horizontal: commonWidth, vertical: 18.0);
